# 🚀 دليل النشر خطوة بخطوة

الوقت المتوقع: **~10 دقائق**. لا يتطلب أي خادم مدفوع.

---

## الخطوة 1 — إنشاء قاعدة البيانات (Google Sheets)

1. افتح [sheets.new](https://sheets.new) لإنشاء جدول بيانات جديد.
2. سمِّه مثلاً: `Masajid Care DB`.
3. لا حاجة لإنشاء الأعمدة يدوياً — سيتم إنشاؤها تلقائياً لاحقاً.

---

## الخطوة 2 — إضافة كود Apps Script

1. من داخل الـ Sheet: **الإضافات (Extensions) → Apps Script**.
2. احذف ملف `Code.gs` الافتراضي.
3. لكل ملف داخل مجلد `gas/` في هذا المستودع:
   - أنشئ ملفاً جديداً بنفس الاسم (أيقونة **+ → Script**).
   - انسخ محتوى الملف والصقه.
4. الملفات المطلوبة (13 ملف):
   `APIDispatcher` · `Config` · `Utils` · `Shared` · `Auth` · `Dashboard` ·
   `Mosques` · `Visits` · `Reports` · `Maintenance` · `Cleaning` · `Assets` ·
   `Notifications` · `Users` · `Setup`
5. احفظ المشروع (💾).

> 💡 بديل احترافي: استخدم [`clasp`](https://github.com/google/clasp) لرفع مجلد `gas/` دفعة واحدة:
> `clasp clone <scriptId>` ثم انسخ `.gs` و `clasp push`.

---

## الخطوة 3 — التهيئة (Setup)

1. في محرر Apps Script، اختر الدالة `setup` من القائمة المنسدلة أعلى المحرر.
2. اضغط **Run ▶**.
3. وافق على صلاحيات الوصول للـ Sheet عند الطلب (المرة الأولى فقط).
4. سيتم:
   - إنشاء كل الشيتات بأعمدتها.
   - إنشاء حساب المدير الافتراضي: **admin@masajid.local / admin123**.
5. (اختياري) شغّل `seedDemoData` لإضافة بيانات تجريبية.
6. (اختياري) شغّل `installDailyTrigger` لتفعيل توليد التنبيهات الدورية يومياً.

---

## الخطوة 4 — النشر كـ Web App

1. أعلى يمين المحرر: **Deploy → New deployment**.
2. اختر النوع: **Web app**.
3. الإعدادات:
   - **Execute as:** `Me` (أنت).
   - **Who has access:** `Anyone` (مطلوب لكي تصل الواجهة للـ API).
4. اضغط **Deploy** وانسخ رابط الـ **Web app URL** (ينتهي بـ `/exec`).

> 🔐 ملاحظة أمان: "Anyone" يعني أن نقطة الـ API عامة، لكن كل العمليات الحساسة
> محمية بالتوكن والصلاحيات داخل `APIDispatcher`. لا تُخزَّن كلمات المرور كنص صريح
> (تُجزّأ بـ SHA-256 + salt).

---

## الخطوة 5 — ربط الواجهة بالـ API

1. افتح `js/config.js`.
2. ضع الرابط المنسوخ في `API_URL`:

```js
window.APP_CONFIG = {
  API_URL: 'https://script.google.com/macros/s/AKfy.../exec',
  ...
};
```

3. احفظ وادفع التغيير إلى GitHub.

---

## الخطوة 6 — تفعيل GitHub Pages

1. في المستودع على GitHub: **Settings → Pages**.
2. **Source:** `Deploy from a branch`.
3. اختر الفرع (مثل `main`) والمجلد `/ (root)`.
4. احفظ، وانتظر دقيقة. سيظهر رابط الموقع:
   `https://<username>.github.io/3nayhmosqe/`

---

## الخطوة 7 — الدخول الأول

1. افتح رابط الموقع.
2. سجّل الدخول: `admin@masajid.local` / `admin123`.
3. من قائمة المستخدم أعلى الصفحة → **تغيير كلمة المرور** فوراً.
4. من **المستخدمون** أضف بقية أعضاء الفريق وحدّد أدوارهم.

---

## 🔄 تحديث الكود لاحقاً

- **تعديل الواجهة:** ادفع إلى GitHub فقط — Pages يحدّث تلقائياً.
- **تعديل الخادم (gas):** بعد لصق التعديل في Apps Script، اعمل
  **Deploy → Manage deployments → Edit → New version** لتطبيق التغيير.

---

## ❓ حل المشكلات الشائعة

| المشكلة | الحل |
|--------|------|
| `لم يتم ضبط رابط الـ API` | لم تعدّل `API_URL` في `config.js`. |
| `تعذّر الاتصال بالخادم` | تحقق أن النشر **Anyone** وأن الرابط ينتهي بـ `/exec`. |
| تظهر صفحة تسجيل دخول Google | النشر ليس "Anyone" — أعد خطوة 4. |
| التغييرات على gas لا تظهر | لم تنشئ **New version** في Manage deployments. |
| `الجلسة غير صالحة` | انتهت مدة الجلسة (12 ساعة) — سجّل الدخول مجدداً. |
