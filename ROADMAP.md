# 🗺️ خطة التطوير طويلة المدى

النظام الحالي **MVP متكامل وقابل للتشغيل**. هذه خطة التوسعة ضمن نفس البنية
(GitHub Pages + Apps Script + Sheets) دون أي خدمات مدفوعة.

---

## ✅ المرحلة 1 — الأساس (مكتملة)
- بنية المشروع + APIDispatcher (Router) + المصادقة والجلسات والصلاحيات.
- لوحة تحكم بإحصائيات ومخططات.
- مخطط قاعدة البيانات الكامل + التهيئة التلقائية.

## ✅ المرحلة 2 — الوحدات التشغيلية (مكتملة)
- إدارة المساجد (CRUD كامل + موقع جغرافي + صور).
- الزيارات الميدانية مع التقييمات.

## ✅ المرحلة 3 — المتابعة (مكتملة)
- البلاغات مع دورة حياة الحالة (جديد → تحت التنفيذ → مكتمل/مؤجل).
- الصيانة مع التكاليف والمستندات.
- النظافة وجداولها.

## ✅ المرحلة 4 — الأصول والإشعارات (مكتملة)
- جرد الأصول وحالاتها.
- إشعارات داخلية + تنبيهات بلاغات متأخرة + صيانة دورية (Trigger يومي).
- سجل تدقيق (AuditLog) لكل العمليات.

---

## 🔜 المرحلة 5 — التقارير المتقدمة
- **تقارير PDF** في المتصفح عبر مكتبة خفيفة (jsPDF / window.print مع CSS طباعة).
- تقارير حسب المسجد / الفترة / نوع البلاغ.
- تصدير CSV/Excel مباشرة من الواجهة.
- *الواجهة جاهزة بنية الفلترة (from/to/mosqueId) في كل وحدة.*

## 🔜 المرحلة 6 — رفع الصور والمرفقات
- رفع الصور إلى **Google Drive** عبر Apps Script (base64 → DriveApp).
- تخزين روابط Drive في أعمدة `Images`/`Documents` الموجودة أصلاً.
- معرض صور لكل مسجد/بلاغ.

## 🔜 المرحلة 7 — تحسين تجربة الميدان
- وضع **PWA** (تثبيت على الجوال + عمل بدون إنترنت جزئياً عبر Service Worker).
- التقاط الموقع الجغرافي تلقائياً أثناء الزيارة (Geolocation API).
- نماذج محسّنة للجوال.

## 🔜 المرحلة 8 — لوحات وتحليلات أعمق
- مخططات تفاعلية (Chart.js) بدل المخططات البسيطة الحالية.
- مؤشرات أداء (KPIs): متوسط زمن إغلاق البلاغ، أكثر المساجد بلاغات...
- خرائط حرارية للبلاغات حسب الحي.

## 🔜 المرحلة 9 — الأتمتة والتكامل
- إشعارات بريد إلكتروني عبر `MailApp` للبلاغات الحرجة.
- تكامل واتساب/رسائل عبر خدمات مجانية (اختياري).
- نسخ احتياطي تلقائي للـ Sheet إلى Drive (Trigger أسبوعي).

## 🔜 المرحلة 10 — الحوكمة والجودة
- صفحة عرض سجل التدقيق (AuditLog) للمدير.
- نظام أذونات أدق على مستوى الحقول.
- اختبارات تلقائية لدوال الـ Backend عبر دالة `runTests()` في Apps Script.

---

## 🧠 إرشادات التطوير المستقبلي بالذكاء الاصطناعي

عند طلب تطوير ميزة جديدة، يكفي توجيه الـ AI كالتالي:

1. **حقل/جدول جديد؟** عدّل `SHEETS` في `gas/Config.gs` فقط (أضِف العمود في النهاية).
2. **عملية API جديدة؟**
   - أنشئ `handleModule_action(payload, user)` في ملف الوحدة المناسب.
   - سجّلها في `getRoutes_()` داخل `APIDispatcher.gs` مع الأدوار المسموحة.
3. **صفحة/وحدة جديدة؟**
   - أنشئ `module.html` (انسخ أي صفحة موجودة) + `js/pages/module.js`.
   - أضِف عنصراً في مصفوفة `NAV` داخل `js/layout.js`.
4. **حافظ على النمط:** الاستجابة الموحّدة `{ok,data,error}`، التحقق بـ `requireFields_`،
   التسجيل بـ `audit_`، والصلاحيات عبر `Auth.can()` في الواجهة.

> كل ملف يحتوي تعليقات عربية تشرح الغرض ونقاط التوسعة — مصمّم ليُقرأ ويُطوَّر آلياً.
